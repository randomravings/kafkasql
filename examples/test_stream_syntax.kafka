create context test;

use context test;

create type Customer as struct (
    Id INT32,
    Name STRING
);

create stream Events (
type InlineEvent as struct (
    EventId INT32,
    EventType STRING
),
type CustomerEvent as test.Customer
);
