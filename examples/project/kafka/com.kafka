-- Context declaration file for the 'com' context.
-- Lives at kafka/com.kafka (depth 1 — no folder constraint).
-- Sub-contexts (e.g. com.example) are declared inside the com/ folder.

CREATE CONTEXT com;
