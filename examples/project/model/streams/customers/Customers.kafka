INCLUDE '../../contexts/customers.kafka';
INCLUDE '../../types/customers/Customer.kafka';

USE CONTEXT customers;

CREATE STREAM Customers (
    TYPE CustomerRecord AS STRUCT (
        Key   INT32,
        Value customers.Customer
    )
    DISTRIBUTE BY (Key)
)
COMMENT 'Main customers stream';
